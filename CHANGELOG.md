# Changelog - Новые возможности Sirius DMS

## 🆕 Последние обновления

### ✅ Завершено

#### 1. **Модальное окно предпросмотра документов**
- **Описание**: При нажатии на документ в чате или в библиотеке открывается модальное окно с предпросмотром
- **Файлы**:
  - `src/components/DocumentPreviewModal.tsx` - новый компонент модального окна
  - `components/ChatInterface.tsx` - интеграция в чат
  - `components/DocumentLibrary.tsx` - интеграция в библиотеку документов
- **Функционал**:
  - Кнопка "Предпросмотр" вместо "Скачать"
  - Модальное окно с возможностью скачивания
  - Placeholder для будущей поддержки различных форматов (PDF, DOCX, TXT, JSON и др.)

#### 2. **Множественная загрузка файлов**
- **Описание**: Возможность загружать несколько файлов одновременно
- **Файлы**: `components/UploadScreen.tsx`
- **Функционал**:
  - Drag & drop для множественной загрузки
  - Выбор нескольких файлов через диалог
  - Параллельная загрузка с отображением прогресса для каждого файла

#### 3. **Проверка дубликатов файлов**
- **Описание**: Система проверяет наличие файлов с таким же именем перед загрузкой
- **Файлы**:
  - `backend/app/api/v1/documents.py` - эндпоинт `/check-duplicate` и проверка при загрузке
  - `components/UploadScreen.tsx` - проверка на фронтенде
  - `api/services/documents.ts` - метод `checkDuplicate`
- **Функционал**:
  - Проверка имени файла в Postgres по названию документа
  - Предупреждение пользователя о существующих файлах
  - Отклонение загрузки дубликатов с 409 Conflict

#### 4. **Celery для фоновой обработки RAG**
- **Описание**: Обработка документов через RAG теперь выполняется в фоновом режиме через очередь задач
- **Файлы**:
  - `backend/app/core/celery_app.py` - конфигурация Celery
  - `backend/app/tasks/rag_tasks.py` - задача обработки документов
  - `backend/app/tasks/__init__.py` - экспорт задач
  - `docker-compose.yml` - добавлен сервис `celery_worker`
  - `backend/requirements.txt` - добавлены зависимости Celery
- **Функционал**:
  - RAG обработка документов в фоновом режиме
  - Очередь задач через Redis
  - Worker с concurrency=1 для экономии памяти GPU
  - Автоматический fallback на FastAPI BackgroundTasks если Celery недоступен

#### 5. **Доступ из локальной сети**
- **Описание**: Система доступна с любого компьютера в локальной сети
- **Файлы**:
  - `backend/app/main.py` - CORS настроен на `allow_origins=["*"]`
  - `backend/app/core/config.py` - расширен список CORS origins
  - `vite.config.ts` - уже был настроен на `host: '0.0.0.0'`
  - `NETWORK_ACCESS.md` - инструкция по подключению
- **Функционал**:
  - Backend слушает на `0.0.0.0:8000`
  - Frontend слушает на `0.0.0.0:5173`
  - Любой компьютер в локальной сети может подключиться по IP адресу

#### 6. **Исправлена генерация ответов модели**
- **Описание**: Убраны повторяющиеся фрагменты в ответах AI
- **Файлы**:
  - `backend/app/api/v1/chat.py` - улучшен промпт, добавлена логика уникальных документов
  - `backend/app/services/qwen_service.py` - добавлен `repetition_penalty=1.1`
- **Функционал**:
  - Краткие и четкие ответы без повторений
  - Корректный подсчет уникальных документов
  - Улучшенное форматирование ответов

#### 7. **Готовность к A100 GPU**
- **Описание**: Система оптимизирована для работы с мощными GPU
- **Файлы**: `backend/app/services/rag_service.py`
- **Функционал**:
  - Автоматический выбор batch size в зависимости от GPU:
    - A100: 128
    - RTX 3090/4090: 64
    - RTX 3060/3070/Ti: 32
    - RTX 2050/2060: 16
    - MPS/CPU: 8
  - Оптимизация использования VRAM
  - Batch processing для эмбеддингов

## 📋 Как использовать новые функции

### Множественная загрузка
1. Перейдите на вкладку "Загрузка"
2. Перетащите несколько файлов сразу ИЛИ нажмите "Выбрать файлы" и выберите несколько
3. Система автоматически проверит дубликаты и загрузит файлы
4. RAG обработка запустится автоматически через Celery

### Предпросмотр документов
1. В чате: нажмите "Предпросмотр" рядом с найденным документом
2. В библиотеке: нажмите "Предпросмотр" в списке или карточке документа
3. В модальном окне: просмотрите информацию и скачайте при необходимости

### Доступ из локальной сети
1. Узнайте IP сервера: `ifconfig | grep "inet " | grep -v 127.0.0.1`
2. На клиенте откройте браузер: `http://<IP_СЕРВЕРА>:5173`
3. Система полностью работает как на локальном компьютере

## 🚀 Запуск системы

### Production (Docker)
```bash
docker-compose up -d
```

Включает:
- Backend API
- Celery Worker
- PostgreSQL
- Redis
- MinIO

### Development (Mac)

**Терминал 1 - Backend:**
```bash
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

**Терминал 2 - Celery Worker:**
```bash
cd backend
celery -A app.core.celery_app worker --loglevel=info
```

**Терминал 3 - Frontend:**
```bash
npm run dev
```

**Терминал 4 - Инфраструктура:**
```bash
docker-compose up postgres redis minio
```

## 🔧 Технические детали

### Архитектура RAG с Celery

```
Пользователь загружает файл
    ↓
FastAPI сохраняет файл в MinIO и создает запись в Postgres
    ↓
Задача на RAG обработку добавляется в Celery очередь (Redis)
    ↓
Celery Worker забирает задачу из очереди
    ↓
RAG: извлечение текста → чанкинг → эмбеддинги (Qwen)
    ↓
Сохранение чанков и эмбеддингов в Postgres (pgvector)
    ↓
Классификация документа через Qwen (теги, резюме)
    ↓
Документ готов к поиску через RAG
```

### Stack
- **Backend**: FastAPI, SQLAlchemy, AsyncPG
- **Frontend**: React, TypeScript, Vite
- **AI/ML**: Qwen 2.5, Transformers, PyTorch
- **Background Jobs**: Celery, Redis
- **Database**: PostgreSQL + pgvector (2560 dimensions)
- **Storage**: MinIO (S3-compatible)
- **Containerization**: Docker, Docker Compose

## 📝 Примечания

- Vector dimension обновлен с 384 на 2560 для поддержки Qwen embeddings
- Celery worker настроен на `concurrency=1` для минимизации использования GPU памяти
- Проверка дубликатов работает по имени файла (title в Postgres)
- CORS полностью открыт для локальной сети (для продакшена рекомендуется ограничить)

